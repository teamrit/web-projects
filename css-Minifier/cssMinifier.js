$("input[type='text']").val("");

$(document).ready(function(){
	$("#convert").on("click", function(){	
		// Alert to box
		alert("asd");
		// Recieve the code from the textbox to string
		var initCode = $("#initCode").val().toString();
		initCode = initCode.replace(/\s/g,'');
		for (var)
		
		console.log(initCode);
		$("#finalCode").val(initCode);
		
	});
});



